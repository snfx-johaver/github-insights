# Sanitized fixtures

Phase 2 and later tests will add synthetic GitHub API fixtures here. Never copy
responses containing tokens, private repository information, signed URLs,
authorization headers, personal data, or Home Assistant storage.

