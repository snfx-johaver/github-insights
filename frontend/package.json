{
  "name": "github-insights-monorepo-frontend",
  "private": true,
  "version": "0.1.0-beta.1",
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "check": "npm run lint && npm run typecheck && npm test && npm run build",
    "lint": "eslint src tests",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-typescript": "^12.1.4",
    "@types/node": "^24.3.1",
    "eslint": "^10.10.0",
    "globals": "^16.3.0",
    "rollup": "^4.63.3",
    "tslib": "^2.8.1",
    "typescript": "^5.9.2",
    "vitest": "^5.0.1"
  }
}
